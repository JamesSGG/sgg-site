
#import "./f-user-info.graphql"
#import "./f-user-emails.graphql"

query User($id: ID!) {
  user(id: $id) {
    ...userInfo
    ...userEmails
    friends {
      ...userInfo
      onlineStatus
    }
  }
}
