
#import "./f-game.graphql"
#import "./f-game-platform.graphql"

mutation CreatePlayedGame($input: CreatePlayedGameInput!) {
  createPlayedGame(input: $input) {
    id
    userId
    gamerTag
    game {
      ...game
    }
    gamePlatform {
      ...gamePlatform
    }
  }
}
