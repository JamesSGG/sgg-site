
#import "./f-game.graphql"
#import "./f-game-platform.graphql"

mutation CreateGamePlayed($input: CreateGamePlayedInput!) {
  createGamePlayed(input: $input) {
    id
    userId
    gamerTag
    game {
      ...game
    }
    gamePlatform {
      ...gamePlatform
    }
  }
}
