
#import "./f-user-info.graphql"
#import "./f-user-emails.graphql"

query CurrentUser {
  currentUser {
    ...userInfo
    ...userEmails

    friends {
      ...userInfo
      lastSeenAt
    }
  }
}
