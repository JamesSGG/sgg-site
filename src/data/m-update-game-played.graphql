
#import "./f-game.graphql"
#import "./f-game-platform.graphql"

mutation UpdateGamePlayed($input: UpdateGamePlayedInput!) {
  updateGamePlayed(input: $input) {
    id
    userId
    gamerTag
    game {
      ...game
    }
    gamePlatform {
      ...gamePlatform
    }
  }
}
