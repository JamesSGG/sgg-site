// @flow

type ReduxState = {
  app: {},
  apollo: {},
}

export const initialState: ReduxState = {
  app: {},
  apollo: {},
}

export default function(state: ReduxState): ReduxState {
  return state
}
